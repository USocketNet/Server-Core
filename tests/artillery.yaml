# Sample load test. $ artillery run artillery.yaml
# Note: unit int are in seconds.
# Note: Make sure on your emit on server to check if function.
# command: artillery run tests/artillery.yaml

config:
  target: "http://usn.mshome.net:6060" #host server
  socketio:
    transports: ["websocket", "polling"] #transport layer.
    query:  # pass query here!
      wpid: "1"
      snid: "raE2AcRdaLSRZ9ygt7m7gwnAZ40iJ8D8oIiVytDXk5p"
      apid: "svr-status"
  phases:
    - duration: 100 # how many seconds to create every seconds.
      arrivalRate: 10 # create virtual users per seconds.
scenarios:
  - engine: "socketio"
    flow:
      - emit:
          channel: "connects"
          data: "{ wpid: 'user-{{ $randomString() }}' }'"
      - loop:
          - emit:
              channel: "pub"
              data: #make an object of emit.
                m: "The message is {{ $randomString() }}!"
          - think: 1 #second before the next send emit.
        count: 60 #how many times this commit be done.
      - think: 60 #time before virtual user disconnect
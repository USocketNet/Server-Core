{
    "production": false,
    
    "allowedDomain": ["localhost"],

    "mysql": {
        "host": "localhost",
        "uname": "admin",
        "pword": "admin",
        "dbase": "usocketnet"
    }
}
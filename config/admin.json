{
    "production": false,
    
    "allowedDomain": ["localhost"],

    "wpress": {
        "host": "localhost/wordpress"
    },

    "mysql": {
        "host": "localhost",
        "uname": "admin",
        "pword": "admin",
        "dbase": "usocketnet"
    }
}